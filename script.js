document.addEventListener('DOMContentLoaded', function () {
  var links = document.querySelectorAll('.index-row');
  links.forEach(function (link) {
    link.addEventListener('click', function () {
    });
  });
});
